# Image Style Templates

This folder is reserved for reusable cover style presets.

The current image agent can work without external templates. Future presets can be
stored here as JSON files and loaded by `generate_prompts.py`.
