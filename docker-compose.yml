services:
  biotech-agent:
    build: .
    image: biotech-agent:latest
    container_name: biotech-agent
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8787:8787"
    volumes:
      - ./database:/app/database
